import { Meta, Source } from '@storybook/blocks';

import { theme } from './theme';

<Meta title="Theme/Theme Object" />

# Theme object

This is basic theme object used by Design System.

<Source code={JSON.stringify(theme, null, 2)} language="json" />
