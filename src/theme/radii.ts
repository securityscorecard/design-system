export const createRadii = (): {
  default: string;
  double: string;
  large: string;
  half: string;
  circle: string;
  round: string;
} => ({
  default: '4px',
  double: '8px',
  large: '16px',
  half: '2px',
  circle: '100%',
  round: '50em',
});
