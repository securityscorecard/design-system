export const createRadii = (): {
  default: string;
  double: string;
  half: string;
  circle: string;
  round: string;
} => ({
  default: '4px',
  double: '8px',
  half: '2px',
  circle: '100%',
  round: '50em',
});
