import { Canvas, Meta } from '@storybook/addon-docs';

import { DSProvider } from '../../theme';
import { Stack } from '../layout';
import {
  Checkbox,
  Error,
  Input,
  Label,
  Note,
  Radio,
  Select,
  Switch,
  TextArea,
} from './index';

export const options = [
  { value: 'HR', label: 'Croatia' },
  { value: 'CU', label: 'Cuba' },
  { value: 'CW', label: 'Cura\u00e7ao' },
  { value: 'CY', label: 'Cyprus' },
  { value: 'CZ', label: 'Czech Republic' },
  { value: 'DK', label: 'Denmark' },
];

<Meta title="components/forms/Example" />

# Form Usage Example

<Canvas>
  <DSProvider>
    <form>
      <Stack gap="md">
        <Stack>
          <Label />
          <Input type="text" isInvalid />
          <Error />
        </Stack>
        <Stack>
          <Label />
          <Select options={options} />
          <Note />
        </Stack>
        <Stack>
          <Label />
          <Stack gap="xs">
            <Radio label="species" name="human" radioId="Human" value="human" />
            <Radio
              label="species"
              name="animal"
              radioId="Animal"
              value="animal"
            />
          </Stack>
        </Stack>
        <Stack>
          <Label />
          <Stack gap="xs">
            <Checkbox
              checkboxId="hobbies"
              label="reading"
              name="Reading"
              value="reading"
            />
            <Checkbox
              checkboxId="hobbies"
              label="sleeping"
              name="Sleeping"
              value="sleeping"
            />
            <Checkbox
              checkboxId="hobbies"
              label="programming"
              name="Programming"
              value="programming"
            />
            <Checkbox
              checkboxId="hobbies"
              label="testing"
              name="Testing"
              value="testing"
            />
          </Stack>
        </Stack>
        <Stack justify="flex-start">
          <Label />
          <Switch label="public" switchId="public" />
        </Stack>
        <Stack>
          <Label />
          <TextArea maxLength="140" />
        </Stack>
      </Stack>
    </form>
  </DSProvider>
</Canvas>
