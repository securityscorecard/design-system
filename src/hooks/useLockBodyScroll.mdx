import { Meta } from '@storybook/blocks';

<Meta title="Hooks/useLockBodyScroll" />

## useLockBodyScroll

This hook is called to prevent scrolling the body of a page while a component is positioned over the page.
Basically, call the useLockBodyScroll hook in any component and body scrolling will be locked until that component unmounts

```js
import { useLockBodyScroll } from '@securityscorecard/design-system';

function ModalWithLockedBody({ title, content, onClose }) {
  // Call hook to lock body scroll
  useLockBodyScroll();

  return (
    <Portal>
      <Overlay placement="center">
        <BaseModal>
          <Content>Modal content</Content>
          <Footer>Modal Footer</Footer>
        </BaseModal>
      </Overlay>
    </Portal>
  );
}
```
