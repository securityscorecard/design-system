export declare const shrinkIfSticky: (sticky?: string) => {
    style: {
        flex: string;
    };
};
export declare const makeStickyColumn: ({ sticky }: {
    sticky?: string;
}) => string;
