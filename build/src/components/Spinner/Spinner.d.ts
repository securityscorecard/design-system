import { SpinnerProps } from './Spinner.types';
declare const Spinner: import("styled-components").StyledComponent<"div", import("styled-components").DefaultTheme, {
    className: string;
} & SpinnerProps, "className">;
export default Spinner;
