import type { Banner } from './types';
import { InstanceId } from '../common/types';
export declare const addBanner: (banner: Required<Banner>) => void;
export declare const removeBanner: (id: InstanceId) => void;
